/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import BottomNavigation from './js/navigator/BottomNavigation';

AppRegistry.registerComponent(appName, () => BottomNavigation);
